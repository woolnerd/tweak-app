import FixtureUniverse from "../fixture-universe.ts";
import Fixture from "../fixture.ts";
import Show from "../show.ts";

const universe = new FixtureUniverse(1);

const tenChannelFixture = new Fixture("123");

const show = new Show();

describe("show", () => {
  it("", () => {});
});
