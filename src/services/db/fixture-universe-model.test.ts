import FixtureUniverseModel from '@/services/db/fixture-universe-model';

describe('it creates a fixture universe database', () => {
  it('creates a table', () => {
    // FixtureUniverseModel.createTable();
  });
});
